import * as React from "react";
import "./index.scss";
import { CommonProps } from "../common_types";
interface MinimalProps extends CommonProps {
}
declare function Minimal(props: MinimalProps): React.ReactElement;
export default Minimal;
